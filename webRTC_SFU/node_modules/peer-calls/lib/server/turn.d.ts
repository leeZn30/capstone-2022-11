import { ICEServer } from './config';
export interface Credentials {
    username: string;
    credential: string;
}
export declare function getCredentials(name: string, secret: string): Credentials;
export declare function processServers(iceServers: ICEServer[]): {
    url: string;
    urls: string | string[];
    username: string;
    credential: string;
}[];
//# sourceMappingURL=turn.d.ts.map