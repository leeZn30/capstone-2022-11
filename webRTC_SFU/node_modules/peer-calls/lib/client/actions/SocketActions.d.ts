import { Store, Dispatch, GetState } from '../store';
import { ClientSocket } from '../socket';
export interface SocketHandlerOptions {
    socket: ClientSocket;
    roomName: string;
    stream?: MediaStream;
    dispatch: Dispatch;
    getState: GetState;
    userId: string;
}
export interface HandshakeOptions {
    socket: ClientSocket;
    store: Store;
    roomName: string;
    userId: string;
    stream?: MediaStream;
}
export declare function handshake(options: HandshakeOptions): void;
export declare function removeEventListeners(socket: ClientSocket): void;
//# sourceMappingURL=SocketActions.d.ts.map